打家劫舍：
dp含义：dp[i]表示从前i（包含i）个家偷的最大值，i从0开始，因为索引从0开始
递推公式：dp[i] = max(dp[i-1], dp[i-2]+nums[i])有偷第i个和不偷第i个两种情况，取最大值。偷第i个则第i-1个不能偷，不偷第i个则第i-1个可以偷(但不一定被偷，这里要弄清楚dp含义)。
初始化：因为dp[i]取决于前两个的dp状态，所以初始化dp[0]和dp[1]。dp[0]只考虑索引为0的第一家，所以肯定偷；dp[1]表示考虑前两家所以取最大的一家。
       dp[0]=nums[0]; dp[1]=max(nums[0], nums[1])
遍历：因为取决于前面的状态，所以从小到大遍历。
